#include <iostream>

using namespace std;

/**
 * @details 定义两个整型变量x和y，double类型变量u和v，读入用户的输入，计算这四个值的均值并且将其四舍五入输出。
 * 四舍五入不可以使用任何条件判断语句(if-else, switch-case, ()? : )，只可以使用static_cast<>()语句
 */


int main() {
    int x, y;
    double u, v;

    // TODO: write your code here

    return 0;
}